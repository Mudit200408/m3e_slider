name: example
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.12.1

dependencies:
  flutter:
    sdk: flutter
  material_ui: ^1.0.0
  m3e_slider:
    path: ../

dependency_overrides:
  m3e_haptics:
    path: ../../m3e_haptics

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
